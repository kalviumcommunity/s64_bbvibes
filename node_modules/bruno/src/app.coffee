
express = require 'express'

log = require './logger'

module.exports = class App

  start: (port=8000) ->
    @server = express.createServer()

    log.debug route

    for route, config of @routes
      log.debug "Adding #{route}"

    log.info "Bruno Application server listening on port #{port}"
    @server.listen port